Goal: Keep every data structure time- and space-efficient with native JavaScript types and minimal code.
Stale code, documentation, and ideas are not allowed and must be removed for good across the project.
In comments document the code goal and tradeoffs that lead to the design choices. Do not remove comments unless stale or obsolete.
Tests using deliberately small proof-of-work difficulties must derive invalid nonces deterministically, because arbitrary nonces can satisfy the proof by chance and cause random failures.
